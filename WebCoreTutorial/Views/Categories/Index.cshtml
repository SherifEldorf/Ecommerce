@model IEnumerable<WebCoreTutorial.Models.Category>

@{
    ViewData["Title"] = "التصنيفات الاساسية";
}

<div class="container">
    <div class="page-title">
        <h4>التصنيفات الاساسية</h4>
    </div>
    <p>
        <a asp-action="Create">انشاء جديد</a>
    </p>
    <table class="table table-responsive-lg table-light">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.catName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.catName)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.id">تعديل</a> |
                        <a asp-action="Details" asp-route-id="@item.id">تفاصيل</a> |
                        <a asp-action="Delete" asp-route-id="@item.id">حذف</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
