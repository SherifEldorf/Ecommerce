@model WebCoreTutorial.Models.Category

@{
    ViewData["Title"] = "حذف";
}
<div class="container">
    <div class="page-title">
        <h4>حذف تصنيف اساسي</h4>
    </div>

    <h4 class="text-danger">هل تريد حذف هذا التصنيف ؟</h4>
    <div>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.catName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.catName)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="id" />
            <input type="submit" value="حذف" class="btn btn-danger" /> |
            <a asp-action="Index">العودة لقائمة التصنيفات</a>
        </form>
    </div>
</div>
